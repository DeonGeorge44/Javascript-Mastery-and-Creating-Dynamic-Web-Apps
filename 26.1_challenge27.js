//-------------------Exercise based on Ternary Operators-----------------------------------------------------------------------------------------------

//If the Person have money they can "buy produts"
//If the Person doesn't have money "They Should Bring the Money".

let haveMoney = true;

const checkMoney = haveMoney === true ? `Can buy the products` : `They should Bring the Money`;
console.log(checkMoney);

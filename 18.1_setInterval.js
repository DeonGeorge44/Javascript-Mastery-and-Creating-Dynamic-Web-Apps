//-----------setInterval-------------------------
//setInterval is used to reapeatedly execute a function or a block of code for a  specified interval

setInterval(() => console.log(`This function will be executed every 2 seconds`), 2000);

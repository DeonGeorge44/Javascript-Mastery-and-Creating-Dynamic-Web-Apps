//---------------------------------------Challenge Based On For Of Loop------------------------------------------------------------------------

// Iterate over "array1" & log the values using for of loop.

const array1 = ["a", "b", "c"];

for (const elements of array1) {
    console.log(`${elements}`);
}

//Create a variable name (isJsProgrammingLanguage) & store true as a value
//Create a variable name (isJsHard) & store false as a value
//Create variable name (favNum) & store favourite number inside
//Now add 'favNum' with the value of undefined

let isJsProgrammingLanguage = true;
let isJsHard = false;
let favNum = 20;

console.log(favNum + undefined);
